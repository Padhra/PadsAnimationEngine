#pragma once

class LevelObject //Wrapper class for objects in the game
{

	//type - save different types of object

	//new Model(glm::vec3(0,0,5), glm::toMat4(correctBlender), glm::vec3(.0001), "Models/jumbo.dae", shaderManager.GetShaderProgramID("diffuse"))

	//vec3 position
	//mat4 orientation
	//vec3 scale
	//std::string filepath
	//std::string shader

	//when loading 
};